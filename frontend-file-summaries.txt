{
  "frontend/src/App.tsx": "Main application component with the loan form, download handling, and footer — the \"Advanced Template Designer\" button and its Dialog modal have been removed; all template hooks and the TemplateDesigner import are retained but the UI entry point is gone.",
  "frontend/src/components/FormSection.tsx": "Loan form component using Template (not CustomTemplate) for the customTemplates prop type, with all form sections (applicant, loan details, processing charge, bank account, custom fields, document generation).",
  "frontend/src/components/TemplateDesigner.tsx": "Advanced template designer with the original TemplateDesignerProps interface (builtInTemplates, onApplyHeaderToAll, etc.) but with all header/footer editing sections removed — only watermark, seal, signature, background, and document content sections remain.",
  "frontend/src/components/PreviewDialog.tsx": "Preview dialog rendering Bajaj Finance header/footer/watermark images with the Loan Approval Letter body; fixes text overflow by adding `wordBreak: 'break-word'`, `overflowWrap: 'break-word'`, `overflowX: 'hidden'`, and `boxSizing: 'border-box'` to all content containers and line elements so text stays within the visible margins.",
  "frontend/src/types/form.ts": "TypeScript type definitions for form data — adds `bankAccountNumber`, `ifscCode`, and `upiId` string fields to the `FormData` interface alongside existing loan fields.",
  "frontend/src/types/templates.ts": "Restored original Template type definitions with all original field names (headline, body, logoDataUrl, businessName, businessAddress, watermarkText, footerText, dataUrl, size, position, rotation, fit) plus enabled/color/signatoryName/signatoryTitle optional fields — fully compatible with all existing consumers.",
  "frontend/src/lib/validation.ts": "Form validation utility — removes mobile number, address, and PAN number validation; retains name, loan amount, interest rate, year validations, and adds optional non-negative check for processing charge.",
  "frontend/src/lib/templates/renderTemplate.ts": "Template rendering utility that replaces placeholders in headline and body using formData values; signature is renderTemplate(template, formData) returning {headline, body}.",
  "frontend/src/lib/templates/getTemplate.ts": "Template retrieval and normalization utility with the Loan Approval Letter template updated to use the new body content — Application Number BP874562193045, Loan Number LAG562198743026, full greeting/approval/EMI paragraphs, Processing & Verification section with refundable notice, Bank Account Details section, four-item bullet disclaimer list, and closing — while preserving all dynamic placeholders.",
  "frontend/src/lib/pdf/generatePdf.ts": "PDF generation utility that renders the document to a canvas, extracts JPEG bytes into a typed `Uint8Array<ArrayBuffer>`, hand-crafts a minimal valid PDF binary with a DCTDecode JPEG XObject filling an A4 page, and returns an `application/pdf` Blob using the explicit `ArrayBuffer` backing to satisfy TypeScript's `BlobPart` type constraint.",
  "frontend/src/lib/download.ts": "Browser download utility that triggers file downloads from Blob objects using URL.createObjectURL and an anchor click; passes the filename through as-is without any extension conversion.",
  "frontend/src/hooks/useLocalStorageState.ts": "Custom React hook for persisting and hydrating state to/from localStorage with automatic serialization.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for backend template operations — adds a `throwIfTemplateError` helper that parses the `TemplateResult` discriminated union and throws descriptive `Error` objects for `alreadyExists`, `notFound`, `unauthorizedField`, and `unexpectedError` variants, so mutations properly propagate failures to callers.",
  "frontend/src/index.css": "Global styles with OKLCH color system defining a warm, professional theme with orange/amber accents for both light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom color tokens, font families, and extended theme settings matching the warm orange/amber design system.",
  "frontend/package.json": "Package configuration with jsPDF added as a dependency for client-side PDF generation.",
  "frontend/src/lib/loan/calculateEmi.ts": "EMI calculation utility using the standard loan formula with monthly interest rate and tenure, plus currency formatting helper for Indian Rupees.",
  "frontend/src/hooks/useTemplates.ts": "Template management hook restored with all original exports and correct imports (getCustomTemplates, saveCustomTemplates, normalizeTemplate with single arg, BUILT_IN_DOC_TYPES); serializes/deserializes templates to/from backend GlobalMasterTemplate format.",
  "frontend/src/lib/templateAssets/fileToDataUrl.ts": "Helper utility to convert uploaded image files to data URLs with validation for use in template designer.",
  "frontend/src/lib/templateAssets/positions.ts": "Position preset utilities using the original 'top-left'/'top-right'/'bottom-left'/'bottom-right'/'center' string literals matching the restored PositionPreset type.",
  "frontend/src/lib/qr/qrPayload.ts": "Deterministic QR payload generator that creates consistent QR content from form data and template settings for preview and download.",
  "frontend/src/lib/qr/renderQr.ts": "Client-side QR code renderer using QRCode.js library (loaded via CDN) to generate QR images without network calls, with canvas fallback.",
  "frontend/src/lib/pdf/layout.ts": "A4 page layout constants updated so `MARGINS.top` is 0 and `HEADER_HEIGHT` is 212px (derived from the Bajaj header image aspect ratio at full canvas width), ensuring body content starts immediately below the header image with no gap.",
  "frontend/src/lib/pdf/renderDocumentToCanvas.ts": "Canvas renderer for PDF generation; fixes text overflow by replacing `textAlign: 'center'` with manual left-anchored centering clamped to `MARGINS.left`, adding a `drawWrappedSegments` function for word-wrapping rich financial segments (₹/% highlights) that exceed `contentWidth`, and using `leftX`/`rightX` margin constants consistently throughout all drawing functions.",
  "frontend/src/components/preview/TemplateOverlay.tsx": "Overlay component rendering background, watermark, seal, and signature using original field names (dataUrl, size, position, opacity) with enabled guards so overlays only show when explicitly enabled.",
  "frontend/index.html": "HTML entry point with the QRCode.js CDN script tag removed; only the Vite module script remains.",
  "frontend/src/lib/shareUtils.ts": "New utility module that exports a `sharePdf` function using the Web Share API with graceful fallback (returns false) when sharing is not supported."
}