{
  "frontend/src/App.tsx": "Main application component that wires the form, template designer dialog, and download flow; passes `onSaveBuiltInTemplate` and `onApplyHeaderToAll` to TemplateDesigner, and calls `sharePdf` after a successful download.",
  "frontend/src/components/FormSection.tsx": "Form component for all loan applicant and loan detail fields, with mobile-only Share PDF buttons that download then attempt Web Share API sharing, fully aligned with the existing `FormData` type fields.",
  "frontend/src/components/TemplateDesigner.tsx": "Advanced template designer component fully aligned with the existing `Template`/`CustomTemplate` types; adds a prominent Save button with \"Saved!\" confirmation state, an \"Apply Header to All Templates\" button with AlertDialog confirmation, and uses only fields that exist on the actual type definitions.",
  "frontend/src/components/PreviewDialog.tsx": "Preview dialog with QR imports and state fully removed; uses the updated `TemplateOverlay` without the `qrDataUrl` prop.",
  "frontend/src/types/form.ts": "TypeScript type definitions for form data including loan fields (PAN, loan amount, interest rate, year, monthly EMI), custom fields, and the three loan document types.",
  "frontend/src/types/templates.ts": "Template type definitions with QR-related fields fully removed; retains `PositionPreset`, `BackgroundSettings`, `WatermarkSettings`, `ImageElementSettings`, `Template`, and `CustomTemplate` with all original fields intact.",
  "frontend/src/lib/validation.ts": "Form validation utilities that check required loan fields including PAN number format validation (5 letters, 4 digits, 1 letter) and numeric validations for loan amount and interest rate.",
  "frontend/src/lib/templates/renderTemplate.ts": "Template rendering utility that imports `FormData` from `types/form` (not `types/templates`) and uses safe string defaults to avoid undefined type errors.",
  "frontend/src/lib/templates/getTemplate.ts": "Template retrieval and storage utility with QR fields removed from all default templates and the `normalizeTemplate` function; uses `null`-safe values matching the updated `Template` type.",
  "frontend/src/lib/pdf/generatePdf.ts": "PDF generation utility importing `DocumentType` and `FormData` from `types/form` (not `types/templates`) to fix the missing-export errors.",
  "frontend/src/lib/download.ts": "Browser download utility that triggers file downloads from Blob objects, converting PDF filenames to PNG for canvas-based generation.",
  "frontend/src/hooks/useLocalStorageState.ts": "Custom React hook for persisting and hydrating state to/from localStorage with automatic serialization.",
  "frontend/src/hooks/useQueries.ts": "Placeholder file for React Query hooks (currently empty as all functionality is client-side).",
  "frontend/src/index.css": "Global styles with OKLCH color system defining a warm, professional theme with orange/amber accents for both light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom color tokens, font families, and extended theme settings matching the warm orange/amber design system.",
  "frontend/package.json": "Package configuration with jsPDF added as a dependency for client-side PDF generation.",
  "frontend/src/lib/loan/calculateEmi.ts": "EMI calculation utility using the standard loan formula with monthly interest rate and tenure, plus currency formatting helper for Indian Rupees.",
  "frontend/src/hooks/useTemplates.ts": "Template management hook using the actual `DocumentType` values from `types/form`, with correct localStorage keys matching `getTemplate.ts`, and an `applyHeaderToAllTemplates` function that copies `businessName`, `businessAddress`, and `logoDataUrl` across all built-in and custom templates.",
  "frontend/src/lib/templateAssets/fileToDataUrl.ts": "Helper utility to convert uploaded image files to data URLs with validation for use in template designer.",
  "frontend/src/lib/templateAssets/positions.ts": "Position preset utilities unchanged in logic; re-emitted to confirm `PositionPreset` import from `types/templates` is valid after the type fix.",
  "frontend/src/lib/qr/qrPayload.ts": "Deterministic QR payload generator that creates consistent QR content from form data and template settings for preview and download.",
  "frontend/src/lib/qr/renderQr.ts": "Client-side QR code renderer using QRCode.js library (loaded via CDN) to generate QR images without network calls, with canvas fallback.",
  "frontend/src/lib/pdf/layout.ts": "Centralized A4 page layout constants (dimensions, margins, header/footer heights) for consistent preview and download rendering.",
  "frontend/src/lib/pdf/renderDocumentToCanvas.ts": "Canvas renderer using correct layout exports (`PAGE_WIDTH`, `PAGE_HEIGHT`, `MARGINS`, `CONTENT_AREA`) and importing types from their correct modules; QR rendering step fully removed.",
  "frontend/src/components/preview/TemplateOverlay.tsx": "Overlay component rendering background, watermark, seal, and signature — QR code overlay and `qrDataUrl` prop fully removed.",
  "frontend/index.html": "HTML entry point with the QRCode.js CDN script tag removed; only the Vite module script remains.",
  "frontend/src/lib/shareUtils.ts": "New utility module that exports a `sharePdf` function using the Web Share API with graceful fallback (returns false) when sharing is not supported."
}